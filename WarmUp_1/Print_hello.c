#include <stdio.h>
#include <stdlib.h>

int main(){
    printf("Hello, World!\n");
    return EXIT_SUCCESS; //got this from the chapter 1 notes that were shown in class, so i decided to use this instead of 0
}